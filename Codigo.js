<script src="https://gist.github.com/tu-usuario/gist-id.js"></script>
// Traductor simple de código
function translateCode(languageFrom, languageTo, code) {
    const translatedCode = `Traducción de ${languageFrom} a ${languageTo}: ${code}`;
    console.log(`Traducción realizada: ${translatedCode}`);
    return translatedCode;
}
